on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup lfs
        run: |
          git lfs install
          git lfs pull

      - name: Check files
        run: |
          sha256sum dummy.png dummy.webp
